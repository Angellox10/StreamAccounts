# Copia este archivo a `.env` y ajusta los valores antes de ejecutar la aplicación.
# NO subir `.env` al repositorio público.

DJANGO_SECRET_KEY=django-insecure-replace-this-with-your-secret
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Si usas SQLite (desarrollo), normalmente no necesitas cambiar DATABASE_URL
# Para producción puedes usar DATABASE_URL con Dj-database-url o similar.
# DATABASE_URL=sqlite:///db.sqlite3
